[gd_scene load_steps=3 format=3 uid="uid://bmkgbrswd37va"]

[ext_resource type="Texture2D" uid="uid://d10gaeg6ckyhp" path="res://Images/player/front.png" id="1_fabpd"]

[sub_resource type="GDScript" id="GDScript_jhnul"]
script/source = "extends CharacterBody2D


const SPEED = 300.0
const JUMP_VELOCITY = -500.0

# Get the gravity from the project settings to be synced with RigidBody nodes.
var gravity = ProjectSettings.get_setting(\"physics/2d/default_gravity\")

@onready var texture = $Texture
var front_sprite = load(\"res://Images/player/front.png\")
var side_sprite = load(\"res://Images/player/side.png\")

func _physics_process(delta):
	# Add the gravity.
	if not is_on_floor():
		velocity.y += gravity * delta

	# Handle jump.
	if Input.is_action_pressed(\"jump\") and is_on_floor():
		velocity.y = JUMP_VELOCITY

	# Get the input direction and handle the movement/deceleration.
	# As good practice, you should replace UI actions with custom gameplay actions.
	var direction = Input.get_axis(\"move_left\", \"move_right\")
	if direction:
		velocity.x = direction * SPEED
	else:
		velocity.x = move_toward(velocity.x, 0, SPEED)

	if velocity.x == 0:
		texture.flip_h = false
		texture.texture = front_sprite
	else:
		texture.texture = side_sprite
		if velocity.x < 0:
			texture.flip_h = true
		else:
			texture.flip_h = false
	

	move_and_slide()
"

[node name="Player" type="CharacterBody2D"]
texture_filter = 1
collision_layer = 2
collision_mask = 3
safe_margin = 0.001
script = SubResource("GDScript_jhnul")

[node name="Texture" type="Sprite2D" parent="."]
texture_filter = 1
texture_repeat = 1
texture = ExtResource("1_fabpd")

[node name="Camera2D" type="Camera2D" parent="."]
texture_filter = 1
position_smoothing_enabled = true

[node name="CollisionPolygon2D" type="CollisionPolygon2D" parent="."]
polygon = PackedVector2Array(4, 14, 8, -3, 8, -4, 2, -12, 0, -14, -5, -14, -7, -12, -7, -8, -8, -4, -8, -1, -8, 14)
